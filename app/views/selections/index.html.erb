<h1>Listing Selections</h1>

<table class="table">
  <thead>
    <tr>
      <th>Owner</th>
      <th>Album</th>
      <th>Photos</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @selections.each do |selection| %>
      <tr>
        <td><%= selection.user.mail %></td>
        <td><%= selection.album.title %></td>
        <td><%= selection.photos.count %></td>
        <td><%= link_to 'Show', selection %></td>
        <td><%= link_to 'Edit', edit_selection_path(selection) %></td>
        <td><%= link_to 'Destroy', selection, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
