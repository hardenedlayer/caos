<div>
  <h1>Albums</h1>
  <div>
  <% @albums.each do |album| %>
    <div><%= link_to album.title, album %></div>
  <% end %>
  </div>
</div>
<div>
  <h1>Selections</h1>
  <div>
  <% @selections.each do |selection| %>
    <div><%= link_to "#{selection.album.title}##{selection.id}", selection %></div>
  <% end %>
  </div>
</div>
